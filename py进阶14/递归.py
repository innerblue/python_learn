"""
什么是递归：
在满足条件的情况下，函数自己调用自己的一种特殊编程技巧

递归注意事项：
注意退出条件，避免变成无限递归
注意返回值的传递，确保从最内层，层层传递到最外层

os模块的三个方法：
os.listdir 列出指定目录下的内容
os.path.isdir 判断给定路径是否是文件夹，返回bool
os.path.exists 判断给定路径是否存在，返回bool
"""
import os
